# Weekly deep sweep — 2026-09-07

## DEAL DECISION COULD CHANGE — three items, none flips a verdict

**1. Eden's "$1,559,000 = minimum to pursue" is not a defensible walk-away.**
That rung is solved on the seller's carried insurance, **$1,060/unit** — below
both the house default ($2,000, raised specifically for the LA crisis) and the
deal's own stated gate (≤$1,600/unit, still open: the `apartment-guard` todo is
unsent). Re-solving the ladder on honest insurance:

| basis | 13% rung | 16% rung | 22% rung |
|---|---|---|---|
| as carried ($1,060/u) | **$1,559,000** | $1,443,000 | $1,251,000 |
| deal's own gate ($1,600/u) | $1,455,000 | $1,347,000 | $1,169,000 |
| house defaults (ins $2,000 + mktg $400 + G&A $480) | $1,246,000 | $1,155,000 | $1,004,000 |

At $1,559,000 the deal returns 13.01% as carried, **10.46%** at the deal's own
insurance gate, 5.39% on house defaults. The published walk-away is overstated
by **$104K–$313K**. **PURSUE is unchanged** — at the real pursue basis
($1,251,000) it returns 22.04% / 19.06% / 12.88% across those three bases, and
the 12.88% is before the seller carry the strategy assumes. The verdict holds;
the negotiation ceiling does not.

**2. Baker's market-approach read was 30% too low.** `hedonic.predict` applied a
year coefficient that the module's own fit output calls invalid in capital
letters (*"captures market-composition shift, NOT real appreciation. Do NOT read
it as a time trend"*) — a ×0.644 haircut at 2026. Baker's 12 units read
**$499,969** against **$709,198** without it, so the market approach appeared to
corroborate a sub-$500K income-approach ladder on the strength of an artifact.
Fixed. Baker stays PASSED (that call rests on 4/12 long-term vacancies and 1984
vintage, not on the hedonic), but the corroboration was false.

**3. Tremé was valued against Baton Rouge comps.** The IC memo picked its comp
market by searching deal history for the substring `"baker"` — and the
2026-08-24 sweep note on Tremé contains *"the 46pct at which **Baker** was
PASSED"*. A Tremé building read **$336,170 / $42,021 per unit** instead of
**$664,377 / $83,047**, 49% low, on a deal asking $106K/unit, in direct
contradiction of CLAUDE.md's rule that the Baton Rouge per-unit screen does not
transfer to New Orleans. Fixed: market now resolves from the deal's parish.

**Also, and separately: Eden's IC memo and bank package both reported a
satisfied insurance gate that is open.** See F2 below. No offer has been made,
so nothing has gone wrong yet — but that gate is one of CLAUDE.md's four
offer-stage hard gates, and the bank package would have carried the claim to a
lender.

---

Baseline: `docs/sweeps/sweep-2026-08-24.md`. **No Python file changed between
that sweep and this one** — only data files (board.html, todos.json,
observations.jsonl, triage notes) — so priority went to depth on paths the last
sweep did not reach, plus its deferred list. Six layers audited in parallel
(engine, MC, ingestion, data-tools, reporting, data-consistency).

Standard, unchanged since 2026-08-09: every finding REPRODUCED or concretely
TRACED with real numbers before it was reported or fixed.

## Suite status

Start: pymodel 4/4 deals, MC 79/79, regressions all pass. End: same three green,
with the regression suite grown from 85 to **133 assertions**.

**~45 findings across the six layers. 20 fixed with reproductions and
regression tests; the rest are listed under Deferred with their numbers.** The
volume is not a sign the codebase decayed — no code changed in two weeks. It is
what a deeper pass finds once the shallow bugs are gone.

---

## Fixed (each REPRODUCED, each with a regression test)

### F1 — `intake.py --apply` crashed and destroyed the deal workbook on the way out · CRITICAL
`tools/intake.py:310`. `defaults.resolve(args.model)` (four args required) sat
behind `hasattr(defaults, "resolve")`, which is always True. Every `--apply` on
a rent roll containing a vacant unit raised `TypeError` — **after**
`clone_model()` had copied the blank master over the deal workbook and
**before** `w.save()`. Reproduced on a copy:

| `deal-intake/baker-trails/baker-trails_acq.xlsx` | before | after |
|---|---|---|
| price | $750,000 | **$600,000** (master demo) |
| mix | 9× 2BR @ $800, 3× 3BR @ $900 | **4× 1BR @ $900, 4× 2BR @ $1,100** |

That workbook is what `board.py`, `icmemo.py` and `bankpackage.py` read. Live
since the 2026-08-24 sweep introduced it — **two weeks in which baker-trails
could not be re-underwritten at all**, because no test exercised `intake.main()`
(the F5 regression test only called `parsers.parse_rent_roll`).

Fixed twice over: the dead line is gone, and intake now builds into a hidden
staging file and `Path.replace()`s it into position only after every write
succeeds, so no future exception between clone and save can destroy a deal.
Regression test runs `intake.main()` end-to-end against a throwaway copy of the
repo and asserts both the success path and that a *failing* run leaves the
previous workbook intact.

### F2 — the insurance gate closed on prose saying it was open · CRITICAL
`tools/icmemo.py:86`, consumed by `tools/bankpackage.py:77`. `_insurance_noted`
returned True on the bare substring `"bindable"` — a word that appears most
often in the sentence stating the quote is still outstanding. Eden's own note:

> `GATE: bindable habitational insurance <=~1600/u (Apartment Guard follow-up drafted)`

Consequences, both reproduced in generated documents:
- IC memo: `✓ Insurance quote noted in deal history or files`
- Bank package: generated without refusing, labelling the seller's carried
  premium **`insurance $1,060/unit/yr (Louisiana-adjusted)`** — an assertion of
  the *opposite* of the truth, since CLAUDE.md's Louisiana adjustment is $2,000.
  Treme (control) correctly showed the red `TEMPLATE ESTIMATE` banner.

Corroborated independently by the still-open `apartment-guard` todo. Fixed:
evidence must now be an artifact — a quote document filed in the deal folder, or
an explicit `insurance_quote` record in `deals.json`. Free-text divination is
gone. Eden's bank package now refuses (exit 1); `--force` labels it correctly.

### F3 — bank-package diligence gates failed OPEN · CRITICAL
`tools/bankpackage.py:69-79`. Defaults were `rr_status="actual", t12_missing=
False, ins_unquoted=False` under `except Exception: pass` — so any failure in
the gap check handed a lender a package with every gate silently reported clear.
The flood.py degrade-optimistically class (F7, last sweep), in the lender's
document. Now defaults to the UNMET values and prints the exception.

### F4 — `pymodel.run()` silently ignored unknown input keys · HIGH
`tools/pymodel.py:810`. `dict(defaults); update(inputs)` meant a typo'd key was
accepted and never read. Reproduced: `run({**eden, "purchase_price": 1_251_000})`
returned the **ask-price** answer (9.87%) instead of the $1.251M answer
(32.91%) — a 23-point error, silently. The repo's own history records a price
ladder published and retracted over exactly this class of slip (baker-trails,
2026-08-03 "RED-TEAM CORRECTION"). Now raises, naming the key.

*Self-inflicted follow-up, caught by the engine auditor and fixed the same
session:* the first version of this guard omitted `commercial_share`, which
`_load_deal` injects from `deals.json`. Every mixed-use deal — supported,
documented, and inside the NOLA buy box — would have raised in
run/tornado/monte_carlo while `solve_price` (which pops the key) kept working.
Both the acceptance and the misspelling are now under test.

### F5 — the comp market was chosen by searching deal history for substrings · HIGH
`tools/icmemo.py:377`, `tools/briefing.py:157`. See decision item 3 above. Both
now call a single `hedonic.market_for_location()` that resolves parish → market
through `latax`, refuses rather than defaulting to Baton Rouge on an
unresolvable location, and prints its reasoning into the memo.

### F6 — the hedonic year regressor · HIGH
`tools/hedonic.py`. See decision item 2. The sample is perfectly confounded
(2021-22 = 31 Baton Rouge-only sales; 2023-24 = none; 2026 = 24 NOLA/Northshore/
Lafayette-heavy), so the year term measured market mix. Dropped from the design
matrix: R² 0.243 → 0.204, SE 0.3584 → 0.3639, non-Baton-Rouge markets move under
2.5%, Baton Rouge moves +41.9%. `--year` is accepted for compatibility and now
says explicitly that it has no effect.

Also added: a **thin-support caveat**. The Northshore coefficient is fit on
**n=1** (Palmetto Greens, 144 units, $15.2M) — a one-observation dummy fits that
sale exactly, so `predict("northshore", 18)` was returning that sale's price per
unit dressed as a market estimate, with `log(units)` nearly flat (doubling units
= ×0.986) so it barely discounted 144 → 18. Covington is a named target
submarket, so this one was live.

### F7 — the IC memo's thesis was its three OLDEST notes · HIGH
`tools/icmemo.py:281`. `thesis_notes[:3]`. Treme's memo printed
**"VERDICT PURSUE"** from 2026-08-08 while the operative 2026-08-24 note —
*"HOUSE RULE NOW FAILS AT EVERY LADDER PRICE… Recommend NOT offering"* — was
note #4 and never appeared. Eden's thesis quoted a superseded $1.354M pursue
price and "honest NOI 123k / 7.26% cap" against the $1,559,000 / $143,474 /
8.44% in its own table two sections below. Now newest-first, dated, with a count
of what was omitted.

### F8 — `solve_price` discarded the basis it certified · HIGH
`tools/pymodel.py:942`. A sale reassesses, so the solver re-derives taxes and
the exit cap at every trial price — then returned `{price, irr}` alone.
Consumers re-scored the rung with the **asking price's** tax bill and exit cap
still attached. Measured understatement of `P(IRR ≥ target)`: treme 16%
−3.0pp, baker 13% −3.3pp, **eden 22% −4.9pp** (33.9% → 38.8%; eden's 22% rung is
the live pursue basis), hwy42 16% −5.5pp. Fixed at the root: `solve_price` now
returns `taxes_annual`, `exit_cap` and a `basis_note`, and both `pymodel.main()`
and `board.py` score each rung on it. Verified each rung now re-scores to its own
label (eden 22% → 22.04%, treme 13% → 13.03%).

Board rungs, before → after: eden 39.5/46.7/60.4 → **40.5/48.2/64.1**;
treme 31.7/34.8/41.1 → **34.7/38.8/47.0**. Found independently by three
auditors. No verdict flips — eden's 16% rung is still under 50%.

### F9 — `portfolio_sim` charged idle capital twice · CRITICAL
`tools/portfolio_sim.py:202` vs `:242`. Year 0 debited the **full $300,000
commitment**; a later acquisition then debited its **full equity again**,
including the part funded from the idle year-0 cash already charged.
baker-then-fourplex printed:

```
Portfolio IRR: -1.1%   Total profit: -$23,995   Cash returned: $437,208 (1.46x)
```

self-contradictory on its face. $143,747 was double-charged. On a
capital-called convention the same sequence is **+9.0% IRR / +$137,533**. The
owner's stated strategy is "maximum units/scale as fast as possible" and this is
the only tool that scores it — it was reporting the sequential path as losing
money. Also fixed: `running_bal` is now credited when a gap injects new capital
(eden-solo printed a −$3,585 year-0 balance against a gap it had just flagged as
funded).

### F10 — `portfolio.py status` booked loan draws as rent · MEDIUM
`tools/portfolio.py:311`. The `NON_OPERATING` exclusion was applied only on the
negative branch. A $25,000 construction-loan draw plus a $3,000 insurance refund
reported NOI **$29,020** against a true operating **$1,020**, flipping the
month's on-track verdict from NO to YES. Positive non-operating credits are now
excluded and reported on their own line; a credit in an expense category is
booked as a refund against that expense, not as collections. No owned deal yet,
so nothing is currently wrong — certain the first month a real bank export is
logged.

### F11 — the IC memo's sensitivity table led with an upside and cut the only vacancy stress · MEDIUM
`tools/icmemo.py:328`. `tresults[:3]` off an absolute-delta sort put
"insurance −50%" (an upside, and $1,500/unit against CLAUDE.md's $3,000 for
Orleans wind) at the head of Treme's table and dropped `vacancy +3pts` — three
lines above the memo's own note that *"vacancy risk is judged by deterministic
stress grid, not MC probabilities"*, leaving an MC probability as the reader's
sole vacancy number. Direct inversion of the house rule the same paragraph
asserts. Now prints every downside row; upsides are listed separately and
labelled so they cannot lead.

### F12 — `irr_verdict` never applied the 13% pursue floor · MEDIUM
`tools/report.py:39`. 12.5% printed *"REALISTIC BAND (12-14%) — normal for
stabilized LA"*, which reads as acceptable for a number the house rule rejects.
CLAUDE.md is explicit that the floor outranks the bands. Added
`PURSUE_FLOOR/STRONG/IDEAL` to `cellmap.py` and a
`BELOW PURSUE FLOOR (13%) — PASS under the house rule` branch.

### F13 — `flood.py`'s own CLI still collapsed `sfha=None` to "no" · HIGH
`tools/flood.py:175`. `interpret()` was correct post-F7 and every consumer does
the three-way test — but the CLI, the one place a human runs the tool by hand,
printed `SFHA : no` for `UNMAPPED`, `D`, `AREA NOT INCLUDED`, `OPEN WATER` and
any unrecognised zone. `IC_MEMO_2026-08-02.md:78` is literally `- **SFHA:** No`.
Fixed and extracted into a testable `_sfha_line`.

### F14 — `latax.estimate_tax` accepted "30" for 30% · MEDIUM
`tools/latax.py:119`. `--commercial-share 30` returned **$276,000/yr** on a
$1.5M Gonzales building (16× the truth) and `parceltax.py` printed it with a
straight face; a negative share silently cut the bill below the residential
floor. `defaults.py` already guards its own fraction keys. Now raises outside
[0, 1].

### F15 — `market.py` was missing two parishes while claiming parity with latax · MEDIUM
`tools/market.py:27`. **St. Bernard** (FIPS 22087) and **Plaquemines** (22075)
absent, so `market.py "st. bernard"` and `jobs.py "st. bernard"` both exited 1.
St. Bernard is in the buy box and is the parish the 2026-08-17 three-parish model
settled *into* it (Chalmette clears 13% at $388K) — the one parish that needed
defending on fundamentals was the one that could not be screened. Fails loudly,
so no wrong number was produced. Both added; parity is now asserted by test.

### F16 — board rung verdicts printed the same number for opposite calls · LOW
`tools/board.py:220`. Test was `b > 0.50`, display was `.0f`: 49.6% rendered
"beats index **50%** — FAILS" and 50.4% rendered "beats index **50%** —
clears". Now one decimal, plus an explicit "within MC noise of the 50% line —
treat as a coin flip" marker inside ±3pp (measured MC noise at n=1000 is ~1.15pp
over seeds 1–25).

### F17 — provenance header stated the precedence backwards · LOW
`tools/report.py:247` printed `(deal docs > override > template)`. Behaviour and
README are `--set override > deal docs > template`. Verified live on cannon-rd
that the behaviour is correct; only the header lied.

---

## Deferred, with numbers

Ranked by what would bite first.

**Ingestion — the parsers have real gaps that a new deal package would hit.**
None is live today (every live workbook's figures were hand-verified against its
source this sweep), but all are one broker statement away:
- **Two-year T-12 takes the older column.** `2024 Total | 2025 Total | Variance`
  → parses 2024. Constructed at real Old Galvez magnitudes: opex understated
  **$32,256/yr**, zero notes, `basis` still reads "under total header".
- **12 monthly columns + any 13th column reads December as the annual figure.**
  `len(nums) == 12` exactly. A trailing `% of EGI` column → insurance $1,575
  instead of $18,933, **−92%**.
- **Everything below the first "Total Operating Expenses" row is dropped with no
  note** — violating the README's "nothing is silently dropped". Management fee
  ($8,840) and replacement reserves ($3,000) sit there by convention.
- **A "Units" header on the unit-ID column inflates the count**: 4 units → 606,
  no note, which also flips mgmt 8% → 3% and payroll $0 → $1,715.
- **Part-year T-12 is flagged and then written anyway.** hwy42's Jan–Jun
  statement writes half-size annuals; the note is not a gate.
- **Quarterly statements are falsely flagged part-year** with instructions to
  multiply by 3.
- **Eden's own P&L loses $6,336/yr net**: `Lawn Care` and `Garbage` unmapped,
  and a `+600` water credit added as an expense via `abs(v)` ($1,200 swing).
  Eden's live workbook carries hand-corrected values, so a re-intake would
  clobber them.
- **Unresolvable `--location` leaves the master's demo $7,000 tax in place**
  (`--location "Tremé"` — the deal's actual neighbourhood — understates Orleans
  taxes by $4,206/yr), and **an empty rent-roll parse leaves the master's demo
  unit mix**, producing a plausible-looking model of the wrong building.
- **`--units-override` overrides a present rent roll** rather than only filling
  in when absent.
- **PyMuPDF is not in `requirements.txt`** though README and CLAUDE.md both
  advertise PDF ingestion — `ModuleNotFoundError` on all four real PDFs. Same
  class as last sweep's F0. **`libreoffice-calc` is also absent** in cloud
  sessions, so no `--recalc` path can run here; `recalc.py` fails loudly and
  correctly, but the session-start hook should install both.

**MC — one item is a live inconsistency and should be settled by Anthony, not by
code.** The deterministic engine keeps the workbook's `capex_unit`; the MC
replaces it with an age-hazard draw. Treme: $350/u underwritten vs **$1,642/u**
MC median; baker $250 vs **$1,683**; eden $400 vs $811. At age 56 the hazards
saturate, so it is a near-deterministic re-rate, not a tail. The board renders
"Return at ask 6.6%" beside "MC P50 −4%" with no reconciliation — whichever is
right, one of the two is wrong by 3.9–5.7 IRR points. Turning it off: treme P50
−4.18% → +0.86%, beats-index 25.9% → 33.6%; baker beats 13.1% → 22.3%. **This is
an underwriting decision (what reserve to carry on a 1970 building), not a bug** —
the same call as Eden's marketing gap, and it belongs to Anthony. Flagged, not
silently changed.

Also deferred in MC: `board.py` has **no stress grid at all**, so every verdict
on the page is MC-derived while the house rule says deterministic + stress grid
outranks MC for vacancy risk (icmemo carries the caveat; board does not). The MC
vacancy right tail is architecturally capped near 20% at P99 — baker's own OM
roll shows **33.3%** actual, which the MC cannot represent, which is precisely
why the house rule exists. `fitted_params.json` is confirmed stale (still books
Jan–Jun 2026 as a full-year observation) but **a refit is impossible here** —
the agent proxy 403s FRED — and the impact is bounded: since growth is recentered
on each deal's own underwriting, a ±40% spread swing moves beats-index ≤0.3pp
and P(≥13%) ≤1.4pp. Four `fitted_params.json` blocks are inert, not one
(`rho_*`, `turnover_rate`, `capex_vintage.age_bands`, and the whole `vacancy`
marginal, which is loaded and discarded); the hardcoded Cholesky factor is valid
and PSD but factors a *different* matrix than documented (eg/turnover −0.09 and
vac/turnover +0.15 against a documented 0), and the realized rg/vac correlation
is −0.11 against a documented −0.50. Bounded at ≤1.3pp of beats-index. The
`test_mc.py [3]` correlation test validates the copula on a variable production
throws away, which is why three sweeps missed it.

**Reporting.**
- **Board's curated Eden copy is hand-typed and two of its numbers are wrong.**
  *"Walk if he won't carry AND won't come under $1.45M"* sits three lines below
  the board's own `$1,443,000 — FAILS the house rule`; the deal first clears 50%
  at ~$1,415K. *"Price your $300K closes without seller carry — ~$1,100,000"*
  needs **$305,250** of equity, $5,250 more than his entire capital (true
  zero-reserve ceiling $1,081,081; with the $30K reserve rule, $972,973). Both
  are numbers Anthony negotiates and wires against. Not fixed because the right
  fix is to compute and interpolate them, which is more than a sweep should
  change in copy without him seeing it — but they should be corrected next.
- Payback ledger counts the 8% pref as return **of** capital, uncapped (reports
  127% repaid); `pay` and `log`-ingested distributions double-book.
- Briefing hands over `pursue@$1,559,000` / `pursue@$714,000` with no
  beats-index and no house-rule word — two prices the board rejects.
- No report prints an as-of date for rates, insurance, taxes or rents, while
  board and memo footers stamp today's date and claim "every number recomputed
  from the live models". Prime has been unverified for 21 days (network policy).
- The CLAUDE.md honesty note appears in **none** of board.html, either IC memo,
  or the bank package — the documents that quote 22% and 27.5%.
- Board's dead/shelved copy is never recomputed (`compute_deal` runs only for
  `OVERRIDES` deals), so baker's DEAD_WHY numbers cannot be checked by the code.

**Data tools.**
- **`loans.py` ignores DSCR entirely** — `max_price` is solved from cash alone
  though every program carries a `dscr` field. At $300K cash it prints Local bank
  **$962,140**, which needs an 8.27% going-in cap; the pipeline mean is 6.65%,
  where the truth is **$663,066** (+45% overstated); seller finance +98%. This is
  the tool that answers "what can I buy", and it is answering a looser question.
  Deferred only because the right fix takes a cap assumption that should be
  printed and agreed, not chosen silently.
- **`marketwatch.py report` presents 3-week-old data as current** and hides 13
  consecutive days of total scrape failure; its "2 listings gone → likely
  leased" is fabricated from two known `$1,000 OFF` banner rows that
  `cmd_assumptions` excludes but `cmd_report` does not filter. Underlying
  reality, worth stating plainly: **the market-watch pipeline has never captured
  a single valid rent** — all four sites render prices client-side, so `curl`
  cannot see them.
- RentCast caches carry no fetch date (all 12 are 29–36 days old); `market.py`
  prints no observation dates and hardcodes `endyear 2024-2026` and ACS 2023/2018;
  `jobs.py`'s single-industry warning is structurally blind on the three
  petrochemical parishes it was written for (`disclosed_total` computed and never
  used); `rates.py` returns exit 1 for both "stale" and "unreachable".

**Data consistency.**
- **baker-trails' workbook is still built on the superseded ESTIMATED roll**
  (9× 2BR @ $800 + 3× 3BR @ $900, GPR $118,800 at 7% vacancy) while the record's
  operative PASS note cites the real OM mix (4× 3BR / 8× 2BR, GPR $126K market /
  $79.8K in-place, 8/12 occupied = 33.3% physical vacancy). The carried $118,800
  matches **none** of the three defensible bases. Carried over from last sweep;
  now unblocked, since F1's fix makes `--apply` work — but *which* basis to
  underwrite is a judgment call, and baker is PASSED/watching.
- Eden and Treme both carry **G&A $200/unit** with no source document (Eden's
  P&L has no G&A line; Treme has no T-12 at all) against a $480 template. Treme
  also carries R&M $700 vs $515. Net effect on Treme is small (~$760/yr).
- `covington-2nd`, `cannon-rd` and `weber-city-mhp` carry `min_dscr = 1.25`, not
  the house 1.30. All three are dead; a re-underwrite would inherit the loose gate.

**Workbook formula suspicions — human session, per the house rule. I did not
open either master for writing.**
- **NEW: `Annual CF` column N (year 11) has no exit block.** N46/N47/N48 blank,
  so `hold_years = 11` — the documented length of the model — silently drops the
  entire sale: no proceeds, no cost of sale, no payoff. pymodel returns 13.00%
  for eden at hold 11; the workbook would return a deep loss. `strategies.py`
  caps at 10, so nothing reaches it today.
- **NEW: the IO test is not refi-aware.** `Annual CF!D32/D35` apply the IO
  condition regardless of refi state; pymodel excludes IO post-refi. Diverges
  when `io_years > refi_year`.
- Re-confirmed from 2026-08-24 by reading the formulas: `D48` nets the old
  balance when `refi_year == hold_years`; `D32` has no zero-balance guard;
  `B52` lacks `MAX(0, …)`.

**Engine, lower severity, traced and left:** a renovated unit books 12 months of
rent premium in its turn year (−$37,079 of DSCR-sized loan on the README's own
example — the dangerous half, since year-1 NOI sizes the debt; needs a paired
workbook change, so not an engine-only fix); `--reno-units` with the default
`--reno-per-year 0` funds the budget and books $0 of premium forever, silently
(−5.55 IRR pts); `refi_new_loan` has no `max(0, …)`, so a negative appraisal
turns a real $1.16M mortgage into +$719K of cash; the waterfall deficit warning
fires from inside every MC draw (**4,576 stderr lines from a three-deal board
run, 100% of them this warning**), burying every other diagnostic. The warning
itself was verified correct to the dollar, and **no live deal has a negative
distributable year** at ask, at any ladder rung, at 12–20% vacancy, or at 7- and
10-year holds — so no report currently quotes LP numbers under it.

---

## Verified CLEAN (evidence, not assertion)

- **All three live workbooks tie to the engine exactly** — cached `Inputs!B52/
  B54/B57` match pymodel to the dollar; all 12 Checks OK on each.
- **Every deal location resolves**, and all four CLAUDE.md millages match latax
  exactly (Orleans 131.99 / Jefferson 118.40 / St. Bernard 141.10 / EBR 108.80).
  Validated against a real assessor record: Eden's $24,637 assessment / $2,466
  bill implies 100.0934 mills vs latax's Livingston 100.10 — **0.007% error**.
  No homestead exemption anywhere in the codebase (correct for investment
  property); apartments at 10%, not the 15% commercial ratio.
- **Every non-dead deal has a workbook whose inputs match its record** (Eden's
  18 homes at $1,180 = $254,880 GPR ties the seller's own total; Treme's 8×
  1BR/650sf at $1,400 and its $11,206 Orleans tax bill both reproduce).
  `central-city-2nd` has no workbook but is dead.
- **Board tiles vs engine, both live deals, at ask** — price, units, avg rent,
  growth rates, vacancy, NOI, cap, IRR, DSCR, equity, insurance, taxes: all tie.
- **Whole-model differential test:** an independent from-scratch reimplementation
  of the pro forma, diffed against `run()` over **3,534 randomized cases**
  (refi years 1–10, IO 0–3, amort 3–30, holds 1–10, reno programs) — **zero
  mismatches** on clean inputs. Debt schedule agrees to ±$0.50 across every
  amortization/IO/refi combination.
- **`_irr`** validated against brute-force root scans on 895 randomized vectors:
  worst error 2.3e-09, no missed or phantom roots.
- **`solve_price` bracketing:** IRR is monotone in price on all 8 deals across
  120 price points from 0.30× to 1.02× ask — zero violations, so the bisection
  cannot land on a false boundary.
- **F2 growth recentering holds for all 8 deals**, not just the 3 under test:
  rent growth max |Δ| 5bp, expense growth 5bp, vacancy 21bp.
- **`safe_writer` guarantees verified by attempting the violations** — formula
  cells and non-blue non-declared cells both raise. Both master `.xlsx` md5s
  unchanged after every `--apply` run this sweep.
- **Size-driven defaults at exact boundaries** (payroll 19→$0, 20→$1,715; mgmt
  49→8%, 50→5%, 150→5%, 151→3%), `--set` fraction guards, `--no-defaults`,
  row-capacity drops, newest-date-wins discovery: all correct.
- **RentCast caches resolve for every live deal** (F8's fix holds, including
  eden via the slug-token fallback).
- **`_beats_index`** closed-form normal CDF verified; the deal-side variance
  does enter correctly, through averaging over the empirical IRR sample.
- **`marketwatch` parsing guards**: JSON-LD and heuristic strictly either/or;
  the discount-context regex rejects both historical Creekside false positives.
  `market-data/observations.jsonl`: 70 rows, **0 corrupt, 0 duplicates, 0
  implausible prices**.
- **`bankpackage` refusal + `--force`** behave exactly as documented on treme;
  its numbers tie to the engine.

## Regression tests added

`tools/test_regressions.py` grew a 2026-09-07 block (85 → **133 assertions**):
intake `--apply` end-to-end including the destroy-on-failure case; the insurance
gate against prose, filed documents and `deals.json` records; bankpackage
fail-closed under a broken gap check; the unknown-key guard plus
`commercial_share` acceptance and its misspelling; `solve_price` returning a
basis that re-scores to its own label; parish→market resolution for nine towns
plus loud failure; hedonic having no year column, an inert `--year`, and thin-
support flagging; the flood CLI's three-way SFHA line; latax fraction
validation; market/latax parish parity; the 13% pursue floor; the tornado's
vacancy row surviving; portfolio_sim charging idle capital once; and
`portfolio status` excluding non-operating credits.
